/**
 * Dev launcher stub. For development, use:
 *   npm run build && npm run gui
 *
 * Or for hot-reload workflow:
 *   tsc --watch (in one terminal)
 *   npm run gui   (in another, after initial build)
 */
console.log("For dev: run 'npm run build' then 'npm run gui'");
